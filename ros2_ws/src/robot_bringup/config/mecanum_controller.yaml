mecanum_controller_node:
  ros__parameters:
    cmd_vel_topic: "/mecanum/cmd_vel"
    emergency_stop_topic: "/emergency_stop"
    target_array_topic: "/edulite/target_array"
    wheel_logical_ids: [0, 1, 2, 3]
    # ROS 2 QoSのqueue depth。
    qos_depth: 3

    # [m] 大きくすると同じ車体速度に必要な車輪角速度が小さくなる。
    wheel_radius: 0.15
    robot_length: 0.355
    robot_width: 0.353
    # [rad/s] 4輪の補正後指令の最大絶対値。超過時は全輪を同じ比率で縮小する。

    # 各ホイール速度に掛ける補正係数。配列順序は出力速度と同一。
    velocity_corrections: [1.0, 1.0, 1.0, 1.0]
    # 前後速度の符号
    vx_sign: 1.0
    # 左右速度の符号
    vy_sign: 1.0
    # 旋回速度の符号
    angular_z_sign: 1.0
    # 4輪の最大角速度絶対値。超過時は全輪を同じ比率で縮小する[rad/s]。
    max_wheel_velocity_rad_s: 50.0
